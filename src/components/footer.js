// Define the Footer component
const Footer = () => {
  return <h3>My Footer in another component</h3>; // Render a footer message
};

// Export the Footer component as the default export
export default Footer;