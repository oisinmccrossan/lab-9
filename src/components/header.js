// Define the Header component
const Header = () => {
  return <h1>My Header in another component</h1>; // Render a header message
};

// Export the Header component as the default export
export default Header;